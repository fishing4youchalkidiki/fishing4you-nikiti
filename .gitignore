# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# local development database. Turso is SQLite, so development runs against a
# plain file here rather than the cloud database — which keeps test bookings
# off the live calendar. This repo is public: the file must never be pushed.
local.db
local.db-journal
local.db-wal
local.db-shm

# vercel
.vercel

# typescript
next-env.d.ts
tsconfig.tsbuildinfo
/dist/
/.wrangler/
/outputs/
/work/
/github-upload/

# guest photos awaiting Dimitris' approval — never publish
/photo-review-for-dimitris/
/website-photos-awaiting-dimitris-approval/

# working notes on the client relationship (phone numbers, pending asks) —
# the repo is public, this is not
STATUS.md
